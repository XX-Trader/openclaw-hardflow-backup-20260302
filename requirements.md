# 当前需求基线

更新时间：2026-08-13

## 项目定位

本仓库是工作流基础设施与配置备份，提供通用项目交付工作流、Runtime Profile、控制面工具、配置模板、文档与测试。仓库虽位于量化交易资料目录中，但不包含行情、回测或交易执行程序；父目录名称只是本机存放位置，不构成本仓库的业务范围。

所有能力保持领域中立，以参数、环境变量和项目契约适配通用软件工程问题。

## 功能要求

1. 流水线适用于任意软件项目，不依赖固定行业、仓库名、主机名、账号、频道或目录。
2. 需求、方案、实现、验证、审查、部署、记忆写回和 Git 发布均有结构化产物与失败回流点。
3. 项目目录、Runtime Home、部署命令、烟测命令和连接器 Profile 必须通过参数或环境变量注入。
4. 部署仅在需求明确提出且命令已配置时执行；烟测按项目配置运行。
5. 必改文件、只读来源、参考模式、检查项、运行产物和禁止目标必须分开记录。
6. 核心规划规则不得包含面向某个行业、厂商或历史项目的专用分支。
7. 仓库不得跟踪 Agent 会话转录、运行缓存、机器专属配置、凭证或私密状态。
8. 文档和测试使用通用示例，并覆盖路径解析、风险扫描、计划编译、部署命令、证据输出和 Git 边界。

## 验收标准

- 第一方跟踪文件中无历史项目名、固定主机路径和行业专用实现规则。
- `project_pipeline_entry.py`、`live_runtime_bridge.py` 与 `pipeline_runner.py` 通过语法检查。
- Runtime Profile、入口、证据桥和计划编译器的定向测试通过。
- 仓库策略检查必须覆盖中文路径，并对领域耦合、凭证形态、机器专属路径和失效 owner 引用零容忍。
- README、需求、变更记录、运行手册、部署说明、待办和完成记录相互一致。
- `git diff --check` 通过，暂存区不包含 `.codex-tmp/` 或其他运行产物。
- 提交推送后，本地提交可由远端目标分支回读确认。

## 非目标

- 保存机器凭证、会话记录或真实账号信息。
- 内置某个项目的部署方式、端口、服务名或业务数据契约。
- 把上游 vendor 内容改写成本仓库自有逻辑。
