# Changelog

## 2026-08-14

### Changed

- README 与需求基线改为直接描述通用能力和仓库契约，不再通过父目录业务或排除项解释定位。
- 将残留的 Profile 职责和外部 API 测试样例改为领域中立的项目状态、事件与组件示例。
- 智能路由改为读取目标 `agents` 目录，只向声明且实际存在的 Agent 分发，并报告缺失注册项与未注册目录。
- `auto-fix`、`db-deploy`、`deployment-test` 和 `feature-development` 收敛为通用兼容入口，统一委托项目交付流水线和项目注入命令。
- 用户文档和脚本示例中的固定用户 Home 改为 `~` 或类型化路径占位符；单机运行日志转入归档。
- 全部测试划分为互斥的 `quick` 与 `integration` 两级门禁，并增加 Windows/Linux CI 矩阵。

### Added

- 增加 MIT 许可证、第三方声明、贡献指南、安全报告策略和开发依赖清单。
- 增加可重复运行的 Python/前端 live fixture，真实验证隔离补丁、项目测试、双路审查、写回和本地裸远端发布。
- Runtime 安装器增加变更前快照、回滚预览、最近一次回滚和连续快照回退。

### Fixed

- 移除仓库策略检查对定位说明的特殊豁免，所有第一方文件统一执行领域耦合检查。
- 标识符检查增加下划线与连字符归一化，防止领域词藏在变量名中绕过门禁。
- 修复智能路由器依赖固定用户目录、带点扩展名无法命中、TypeScript 误路由、配置优先级未执行、失效显式目标误接收和未知配置目标等问题。
- 修复当前维护文档中的 31 个失效本地链接，并在仓库策略检查中增加 `broken_markdown_link` 门禁。
- 修复 Git 同步入口在源码检出布局中找不到共享输出模块、导致 `--help` 启动失败的问题。
- 修复 Git 发布敏感信息扫描器把 `${ENV_VAR}` 和类型化 Secret 引用误判成长口令的问题。
- 修复 API 文档门禁根目录少退一级、把普通路由器模块误判为接口代码的问题，并改为可配置的通用源码与文档路径正则。
- 修复 Runtime 安装只有安装清单、缺少可执行恢复路径的问题；回滚会恢复旧文件、移除本次新增文件并保留非托管内容。
- 修复记忆蒸馏器 `--skip-llm` 默认恒真且保留未接入分支的问题，改为显式、可验证的 `rules` 分类器契约。
- 修复跨运行时探测使用当前系统分隔符、技能发现只扫描当前平台目录的问题，并兼容 Windows 临时目录的短路径别名。
- 修复 CI 浅克隆把当前提交误当根提交、进而对全部历史文件执行空白检查的问题；门禁现在只检查本次提交差异。
- CI 官方 Action 升级到 Node.js 24 运行时版本，消除旧运行时弃用告警。

### Removed

- 删除四套重复且带历史 fixture 的实现脚本、模板和工作流，以及智能路由未被实际入口使用的第二套 `lib` 实现。

## 2026-08-13

### Changed

- 将项目交付入口、运行证据桥和 Runtime Profile 改为环境驱动的通用命名。
- 将计划编译、目标分类、API 契约、验证命令和部署执行改为领域中立规则。
- 部署阶段改为显式需求加项目命令触发，不再内置服务、端口或进程管理方式。
- 将外部资料样例改为通用开发平台资料，测试 fixture 改为通用服务和 API。
- 重写 README、需求、运行手册、部署说明、决策、陷阱、待办和完成记录。
- 将分散脚本引用收敛到各 Skill owner，并增加源码检出模式导入引导。
- 新增 `repository_policy_check.py`，以 UTF-8、NUL 分隔的 Git 路径清单检查中文路径、领域耦合、凭证形态、机器路径和失效 owner。
- Agent 索引生成器改为命令行参数或环境变量驱动，不再依赖固定用户目录。

### Fixed

- 修复旧扫描器因 Git 路径转义而漏扫中文文件名的问题。
- 修复多个 owner 脚本在直接执行时先导入依赖、后初始化源码路径造成的 `ModuleNotFoundError`。
- 修复本地 Git 备份与自动更新入口缺少 `sys` 导入的问题。
- 修复到期 TODO 测试依赖实际日期、过期数量随时间增长的问题。
- 修复 PowerShell 节点同步脚本的引号和解析错误。
- 修复 API/SOURCE Registry JSON 模板使用伪布尔联合值、导致模板不能被标准 JSON 解析器读取的问题。
- 修复数据库部署脚本 `case` 语法错误、函数定义晚于入口调用及 `--help` 被数据库配置前置校验阻断的问题。
- 修复 Runtime 安装报告时间戳和 Windows 执行位处理造成的重复安装误报变更，并补充幂等回归测试。

### Removed

- 历史项目专用规则、固定主机路径、具体账号与频道数据。
- 已跟踪的 Agent 会话转录和行业专用测试、报告。
- 面向单个项目的自动目标补全、平台范围和接口特例。
- 已跟踪的认证 Profile、运行快照、控制面 latest/state 产物和机器级服务状态。
- 只覆盖已退役基准编排器、旧 Cron/Profile 安装器及绑定生成器的失效测试。
