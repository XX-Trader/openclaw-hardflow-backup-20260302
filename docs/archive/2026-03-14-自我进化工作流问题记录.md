# 2026-03-14 自我进化工作流问题记录

## 元数据

- 记录性质：运行快照 / 问题观察记录
- 观察时间：2026-03-14 12:04:45（北京时间）
- 观察范围：当前本机 `~/.openclaw` runtime 与本仓工作区
- 相关文档：
  - [2026-03-14-doc-map-agent-workflow.md](./2026-03-14-doc-map-agent-workflow.md)
  - [2026-03-14-agent-skill-hook-绑定现状与优化清单.md](./2026-03-14-agent-skill-hook-绑定现状与优化清单.md)
  - [2026-03-14-external-pattern-learning-pipeline.md](./plans/2026-03-14-external-pattern-learning-pipeline.md)

## 证据来源与复核方式

本文件中的“已确认”结论来自以下证据面：

- `~/.openclaw/cron/jobs.json`
- `~/.openclaw/ops/cron-runs/`
- `~/.openclaw/ops/web-intel/`
- `~/.openclaw/ops/github-web-evolution/`
- `~/.openclaw/ops/git-sync-push/`
- `~/.openclaw/ops/task-center/executor-runs/`
- 本仓 Git 历史

建议复核命令：

```bash
openclaw cron status --json
openclaw cron runs --limit 50
python scripts/openclaw-ops/recover_stale_cron_running_state.py --dry-run
git log --oneline --decorate --grep='self-evolution\|ops-git-sync-push'
ls ~/.openclaw/ops/web-intel ~/.openclaw/ops/github-web-evolution ~/.openclaw/ops/git-sync-push
```

说明：

- 本文记录的是 2026-03-14 的时点状态，不是长期真值。
- 如果后续修复、重装或重跑任务，文中时间点和状态很可能失效，应重新核查而不是默认沿用。

## 1. 记录目的

本文件用于记录 2026-03-14 对当前“自我进化工作流”进行现场核查后的问题结论，范围仅覆盖以下链路：

1. 查询网络外部信息。
2. 生成 agent 优化建议或优化任务。
3. 将自动修改结果提交并推送到远程 Git 仓库。

## 2. 总体结论

结论不是“没有这套能力”，而是“能力已设计并已安装，但当前运行状态不健康”。

已确认：

1. 本机 `~/.openclaw/cron/jobs.json` 中已启用自我进化相关任务。
2. 仓库历史中存在自动提交记录，说明自动 `commit + push` 过去确实跑通过。
3. 当前多数相关任务最近状态为 `error`，部分任务状态存在卡住迹象，未形成稳定闭环。

## 3. 已启用但运行异常的任务

以下任务均已启用，但核查时最近状态不是健康成功：

1. `ops_conversation_evolution_incremental`
2. `ops_governance_evolution_incremental`
3. `ops_git_sync_push`
4. `ops_github_web_evolution_incremental`
5. `ops_self_evolution_weekly_todo`
6. `web_intel_collect_hourly`
7. `web_intel_review_optimization_4h`
8. `web_intel_review_project_docs_6h`
9. `ops_auto_update_install_hourly`
10. `ops_system_schedule_audit`
11. `task_executor_10m`

核查时可见多个任务的 `runningAtMs` 停留在同一时间点 `2026-03-13 15:43:34`，说明至少存在“超时后未正常回收运行态”或“调度状态残留”的问题。

## 4. 三条核心链路的现状

### 4.1 外部信息采集链路

代码与任务入口存在：

1. `scripts/openclaw-ops/github_web_evolution_runner.py`
2. `scripts/openclaw-ops/web_intel_collect_runner.py`
3. `scripts/openclaw-ops/web_intel_review_runner.py`

当前问题：

1. `ops_github_web_evolution_incremental` 已启用，但最近一次运行为 `2026-03-13 13:13:29`，状态为 `error`。
2. `web_intel_collect_hourly` 最近一次运行为 `2026-03-13 09:06:27`，状态为 `error`。
3. `web_intel_review_optimization_4h` 最近一次运行为 `2026-03-13 14:53:31`，状态为 `error`。
4. `web_intel_review_project_docs_6h` 最近一次运行为 `2026-03-13 15:18:31`，状态为 `error`。

已确认的直接故障点：

1. 当前环境变量中未检测到 `GITHUB_TOKEN`。
2. `~/.openclaw/ops/web-intel/state.json` 中存在 `scrapling` 缺失报错。
3. 同一状态文件中存在 Playwright 浏览器可执行文件缺失报错。
4. `~/.openclaw/ops/github-web-evolution/` 下未见有效 `state.json` 与报告文件，说明该链路很可能在入口前就失败。

结论：

外部信息采集能力“有”，但当前不是可稳定运行状态。

### 4.2 优化建议 / 自我进化链路

代码与任务入口存在：

1. `scripts/openclaw-ops/conversation_evolution_runner.py`
2. `scripts/openclaw-ops/self_evolution_todo.py`
3. `scripts/openclaw-ops/governance_evolution_runner.py`
4. `scripts/openclaw-ops/github_web_evolution_runner.py`

当前问题：

1. `ops_conversation_evolution_incremental` 最近一次运行为 `2026-03-13 06:46:26`，状态为 `error`。
2. `ops_governance_evolution_incremental` 最近一次运行为 `2026-03-13 07:16:26`，状态为 `error`。
3. `ops_self_evolution_weekly_todo` 最近一次运行为 `2026-03-10 16:05:29`，状态为 `error`。

行为边界说明：

1. `conversation_evolution_runner.py` 默认是“打包 TODO 任务并等待人工确认”，不是直接自动改高风险内容。
2. `github_web_evolution_runner.py` 默认也是“产出人工审核的优化任务”，不是直接自动改代码。
3. `governance_evolution_runner.py` 具备生成优化任务的能力，也具备可选自动 PR 能力，但当前已安装任务命令未启用 `--auto-pr`。

数据库现状：

1. 任务库中未查询到 `self_evolution`、`conversation_evolution`、`governance_evolution_optimize`、`governance_evolution_review` 的落地任务记录。
2. 这说明“自我进化发现问题 -> 生成后续优化任务 -> 进入执行闭环”这条链路当前没有稳定落地。

结论：

当前更接近“有设计、有入口，但没有稳定产出有效优化任务”。

### 4.3 Git 自动提交与远程推送链路

代码与任务入口存在：

1. `scripts/openclaw-ops/git_sync_push_runner.py`
2. 已安装任务 `ops_git_sync_push`

已确认事实：

1. 当前仓库远程 `origin` 已配置到 GitHub。
2. `ops_git_sync_push` 的实际参数包含 `--auto-pull --push`。
3. Git 历史中存在以下自动提交记录，说明该链路曾真实执行过：
   - `2026-03-05`
   - `2026-03-06`
   - `2026-03-07`
   - `2026-03-08`
   - `2026-03-09`
4. 自动提交信息格式为 `chore(self-evolution): sync updates [cron:ops-git-sync-push] (...)`。

当前问题：

1. `ops_git_sync_push` 最近一次运行为 `2026-03-13 07:56:26`，状态为 `error`。
2. `~/.openclaw/ops/git-sync-push/` 下未见有效状态文件，说明当前任务可能在早期阶段即失败，未稳定产出运行状态。

结论：

自动推送能力“存在且历史上跑通过”，但当前不是正常工作状态。

## 5. 任务执行闭环的补充问题

除自我进化本身外，执行闭环也存在问题：

1. `task_executor_10m` 最近一次运行为 `2026-03-13 10:21:33`，状态为 `error`，连续错误次数较高。
2. 这会影响“上游生成任务后由执行器消费”的后半段闭环。
3. `~/.openclaw/ops/task-center/executor-runs/` 中虽能看到历史执行报告，但最近并未体现出稳定持续消费任务的状态。

## 6. 已确认的历史运行痕迹

虽然当前状态异常，但并非从未运行：

1. `~/.openclaw/ops/system-schedule/snapshots/` 中可见 2026-03-08 的系统调度快照。
2. `~/.openclaw/ops/cron-runs/` 中可见 2026-03-08 至 2026-03-10 的历史运行报告。
3. `~/.openclaw/ops/web-intel/` 中可见 2026-03-07 至 2026-03-08 的采集与审核报告。
4. Git 历史中可见 2026-03-05 至 2026-03-09 的自动同步提交。

这说明问题不是“能力未接入”，而是“此前跑过，但现在退化为异常状态”。

## 7. 当前影响

当前影响可归纳为：

1. 不能依赖该工作流持续、稳定地自动发现外部新信息。
2. 不能依赖该工作流稳定地产生可执行的 agent 优化任务。
3. 不能依赖该工作流持续自动把新改动同步到远程仓库。
4. 即使部分任务被创建，执行器异常也会削弱闭环。

## 8. 后续处理建议

建议按以下顺序排查，而不是同时改多条链路：

1. 先清理卡住的任务运行态，确认调度器还能继续推进。
2. 修复 `web-intel` 运行依赖，包括 `GITHUB_TOKEN`、`scrapling`、Playwright 浏览器运行环境。
3. 手动单跑 `web_intel_collect_runner.py` 与 `github_web_evolution_runner.py`，确认外部信息采集先恢复。
4. 手动单跑 `governance_evolution_runner.py` 与 `conversation_evolution_runner.py`，确认是否能真实产出任务。
5. 单独验证 `task_executor_runner.py`，确认任务创建后能被消费。
6. 最后再验证 `git_sync_push_runner.py`，确认自动提交与推送恢复。

## 9. 本次记录不包含的内容

本文件只记录“已确认问题与现状”，不包含以下动作：

1. 不代表问题已修复。
2. 不代表已加入 `todo.md` 任务看板。
3. 不代表已完成根因定位。

如需把这些问题继续拆成待办任务，应单独补充优先级后再写入任务看板。

## 时效性说明

- 本文是问题快照，不是 runtime 契约文档。
- 需要长期稳定引用的边界与设计，应以 bridge 文档、字段规范和绑定现状文档为准。
- 若未来再次核查，建议新开一份按日期命名的记录文档，避免把多个时点的事实混写到同一份文件。

---

记录时间（北京时间）：2026-03-14 12:04:45
