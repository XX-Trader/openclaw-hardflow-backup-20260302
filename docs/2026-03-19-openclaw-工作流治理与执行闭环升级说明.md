# 2026-03-19 OpenClaw 工作流治理与执行闭环升级说明

更新时间：2026-03-19（北京时间）

## 1. 目标

把当前 `OpenClaw` 工作流从“已能部署、已能运行”升级为“有治理规则、有评分体系、有执行闭环、有验收标准”的完整工程体系。

这次确认的新增内容，属于**工作流更新**，不只是部署说明补充。

因此这些内容应该进入独立的更新文档，而不是继续零散分布在部署文档里。

本次升级目标：

1. 补齐工作流治理层。
2. 补齐执行闭环层。
3. 补齐验收与验证层。
4. 补齐评分与门禁层。
5. 补齐调试与 review 纪律层。

---

## 2. 本次确认需要纳入的工作流更新

### 2.1 HardFlow 评分与门禁系统纳入正式工作流

来源：

- `scripts/hardflow/score-policy.json`
- `scripts/hardflow/SCORECARD_SCHEMA.md`
- `C:/Users/superma/.claude/skills/openclaw-hardflow-automation/SKILL.md`

需要纳入的内容：

1. `G0-G6` 七道评分 gate
2. 每道 gate 的阈值
3. `security` 的 veto 规则
4. `scorecard JSON` 的输出要求
5. gate 失败后的回流整改与重评分

当前已确认规则：

1. `G0 requirements >= 93`
2. `G1 solution >= 92`
3. `G2 frontend >= 92`
4. `G3 backend >= 93`
5. `G4 security >= 95`
6. `G5 release >= 92`
7. `G6 final >= 93`

补充说明：

- 每个 gate 必须独立通过
- 不允许总分抵扣
- `critical/high` 风险未闭环时应 veto

这是当前最关键但之前文档写得不够完整的一项。

2026-03-20 落地进展：

1. 已在 `scripts/hardflow/hardflow-run.sh` 中新增 `workflow` 统一主流程入口
2. 已把 `G0-G6` 所在的 direct 执行链路从长命令串收敛到统一入口
3. 已同步更新 `scripts/hardflow/README.md`，明确 `workflow` 为正式主流程
4. 当前仍需继续把 gate 状态、scorecard 产物和后续验证/验收链路收拢到更明确的编排层

### 2.2 需求质量评分纳入工作流前置阶段

来源：

- `C:/Users/superma/.claude/skills/requirements-clarity/SKILL.md`

需要纳入的内容：

1. 需求质量评分 `0-100`
2. 评分维度
3. 低分继续提问机制
4. 达标后再进入任务分解

建议纳入的评分维度：

1. 商业价值与目标
2. 功能需求
3. 用户体验
4. 技术约束
5. 范围与优先级

建议规则：

- `< 90`：继续澄清
- `>= 90`：允许进入任务分解

这项更新的意义：

- 避免“需求没讲清楚就开始跑 workflow”
- 让需求澄清从经验动作变成制度动作

### 2.3 任务分解与并行执行纳入正式编排层

来源：

- `C:/Users/superma/.claude/skills/task-decomposer/SKILL.md`
- `C:/Users/superma/.claude/skills/parallel-executor/SKILL.md`
- `C:/Users/superma/.claude/skills/smart-workflow/SKILL.md`

需要纳入的内容：

1. 需求澄清后的任务分解逻辑
2. Agent 组合推荐
3. 并行批次划分
4. 执行进度跟踪
5. 阶段化执行与依赖管理

建议纳入的标准链路：

1. 需求澄清
2. 任务分解
3. 并行执行
4. 结果整合
5. 测试与修正
6. 部署验收

这项更新的意义：

- 把 workflow 从“角色列表”升级成“真正可编排的流程”

### 2.4 结果整合与测试-修正循环纳入闭环

来源：

- `C:/Users/superma/.claude/skills/result-synthesizer/SKILL.md`

需要纳入的内容：

1. 多 agent 结果整合
2. 测试前汇总报告
3. 测试失败后的修正循环
4. 修正后再测试
5. 确认无问题后再部署

建议流程：

1. 结果整合
2. 测试
3. 修正
4. 再测试
5. 达标后部署

这项更新的意义：

- 防止“结果一出来就直接进入部署”
- 明确“测试-修改-再测试”是 workflow 的正式一段

### 2.5 部署后验收测试纳入正式验收层

来源：

- `C:/Users/superma/.claude/skills/deployment-test/SKILL.md`

需要纳入的内容：

1. 端口检测
2. API 测试
3. UI 自动化测试
4. 测试报告生成
5. 配置驱动测试

建议验收检查项：

1. 服务端口是否监听
2. 核心 API 是否返回正常
3. UI 页面是否可访问
4. 登录与核心通道是否可用
5. 报告是否生成

这项更新的意义：

- 把“验收”从口头确认升级成标准测试动作

2026-03-20 落地进展：

1. 已在 `scripts/hardflow/hardflow-run.sh` 中新增 `acceptance-test` 阶段
2. 已新增 `scripts/hardflow/check-deployment-acceptance.sh`
3. 当前默认已把以下检查纳入必检项：
   - `openclaw gateway status`
   - `openclaw status`
   - `openclaw plugins list`
   - `openclaw hooks check --json`
   - `openclaw cron status --json`
4. 当前 run 的验收产物固定为 `.workflow/runs/<run_id>/acceptance/deployment.json`
5. 当前已把“部署后验收测试”从建议动作推进成 hardflow 主流程正式阶段

### 2.6 完成前验证制度纳入强制规则

来源：

- `C:/Users/superma/.claude/skills/verification-before-completion/SKILL.md`

需要纳入的内容：

1. 没有新鲜验证证据，不允许声称完成
2. 没有真实命令输出，不允许声称通过
3. 在提交、PR、完成任务前必须验证

建议纳入的核心规则：

- `NO COMPLETION CLAIMS WITHOUT FRESH VERIFICATION EVIDENCE`

这项更新的意义：

- 提高 workflow 的诚实性
- 防止“感觉修好了”“应该没问题了”这种虚假完成

2026-03-20 落地进展：

1. 已在 `scripts/hardflow/hardflow-run.sh` 中新增 `verify-completion` 阶段
2. 已新增 `scripts/hardflow/check-completion-verification.sh`，负责校验当前 run 的 gate、日志与关键产物
3. `git-push` 现在要求当前 run 存在且通过 `.workflow/gates/completion_verification.json`
4. 当前已把“完成前验证”从文档规则推进成主流程里的硬阻断

### 2.7 系统化调试 SOP 纳入运维与排障层

来源：

- `C:/Users/superma/.claude/skills/systematic-debugging/SKILL.md`

需要纳入的内容：

1. 先查根因，再改代码
2. 先复现，再修复
3. 先做数据流定位，再判断故障点
4. 禁止基于猜测做 quick fix

建议纳入的核心规则：

- `NO FIXES WITHOUT ROOT CAUSE INVESTIGATION FIRST`

这项更新的意义：

- 对 `OpenClaw` 特别重要，因为它涉及：
  - gateway
  - hooks
  - plugins
  - channels
  - memory
  - jobs

这些问题天然就是多层系统问题。

### 2.8 代码审查触发规则纳入 workflow 后段

来源：

- `C:/Users/superma/.claude/skills/requesting-code-review/SKILL.md`

需要纳入的内容：

1. 任务完成后 review
2. 大功能完成后 review
3. merge 前 review
4. review 结果进入 gate 体系

这项更新的意义：

- 让 reviewer 不只是角色存在
- 而是真正成为 workflow 的固定动作

## 3. 本次更新后，工作流应具备的完整链路

更新后的目标链路应为：

1. 需求澄清
2. 需求质量评分
3. 任务分解
4. 并行执行
5. 结果整合
6. HardFlow 评分与 gate 判定
7. 测试与修正循环
8. 代码审查
9. 部署
10. 部署后验收测试
11. 完成前验证
12. 多服务器统一治理

这表示：

- 当前 workflow 不只是“把任务派出去”
- 而是“从需求到部署验收的全链路工程流程”

---

## 4. 本次更新后，文档体系中仍缺失的章节

根据这轮排查，当前文档体系还应补下列章节：

1. 评分与门禁系统
2. 需求质量评分系统
3. 任务分解与并行执行规范
4. 结果整合与测试修正循环
5. 部署后验收测试规范
6. 完成前验证规则
7. 系统化调试 SOP
8. 代码审查触发规则

---

## 5. 建议落点

建议把这些更新分别落到两类文档里：

### 5.1 总治理文档

建议新增或扩展一份治理文档，集中承载：

1. 评分与 gate
2. 验证规则
3. review 规则
4. 调试 SOP
5. 测试与验收闭环

### 5.2 backlog 文档

建议把这些新增项同步回写到：

- `docs/2026-03-19-openclaw-workflow-code-optimization-backlog.md`

让 backlog 不只写“部署入口、配置、hooks、jobs”，还要把：

1. 评分体系
2. 验收测试
3. 完成前验证
4. 调试纪律
5. review 触发规则

一并纳入执行计划。

---

## 6. 建议执行顺序

建议新增这些执行顺序：

### 第一批立即补文档

1. 评分与门禁系统
2. 完成前验证规则
3. 部署后验收测试
4. 系统化调试 SOP

### 第二批纳入执行层

5. 需求质量评分
6. 任务分解与并行执行规范
7. 结果整合与测试修正循环
8. 代码审查触发规则

---

## 7. 当前结论

这次排查后可以明确：

1. 这些内容不是“额外补充说明”
2. 这些内容属于真正的**工作流升级**
3. 应该进入独立的更新文档
4. 后续还需要同步进入 backlog 与治理文档

因此这份文档的定位就是：

- `OpenClaw` 工作流治理与执行闭环的升级说明
